/**
 * @fileOverview Firestore Security Rules for the Frotacontrol application.
 *
 * DEVELOPMENT MODE: These rules are temporarily open for development purposes.
 * WARNING: These rules allow anyone to read, write, and delete data in your database.
 * Before deploying to production, you MUST secure your data.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read and write operations to any document in the database.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
